# Archives (submission zips — regenerated as needed, not source)
*.zip

# OS / editor cruft
.DS_Store
Thumbs.db
desktop.ini

# Microsoft Office temp/lock files
~$*

# Local tooling / assistant config
.claude/

# Node (in case any project adds it later)
node_modules/
